---
layout: base
---
{% comment %} `menu` is derived from the file name in the `_data` directory. {% endcomment %}
<h1>{{ menu.title }}</h1>

<ul>
  {% for menuitem in menu.menus %}
  {% endfor %}
    <li>
      <p>{{ menuitem.category }}</p>

     <!-- {% for menuitem.items %} ->>
    </li>
  {% endfor %}
</ul> 
